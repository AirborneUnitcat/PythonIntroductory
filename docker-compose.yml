services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=http://api:8000
    depends_on:
      - api
    restart: always